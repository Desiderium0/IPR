describe('PostComponent', () => {
  it('something', () => {
    expect(1 + 1).toBe(2);
  });
  it('something', () => {
    expect(1 + 1).toBe(2);
  });

  it('something', () => {
    expect(1 + 1).toBe(2);
  });
  it('something', () => {
    expect(1 + 1).toBe(2);
  });
  it('something', () => {
    expect(1 + 1).toBe(2);
  });
  it('something', () => {
    expect(1 + 1).toBe(2);
  });
  it('something', () => {
    expect(1 + 1).toBe(2);
  });
  it('something', () => {
    expect(1 + 1).toBe(2);
  });
  it('something', () => {
    expect(1 + 1).toBe(2);
  });
  it('something', () => {
    expect(1 + 1).toBe(2);
  });
});
